version: 0.0
os: linux 
files:
  - source: /
    destination: /
file_exists_behavior: OVERWRITE

hooks:
  ApplicationStop:
    - location: scripts/stop_app.sh
      timeout: 300
      runas: ec2-user
  ApplicationStart:
    - location: scripts/start_app.sh
      timeout: 3000
      runas: ec2-user
